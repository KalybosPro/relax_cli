name: example
description: A Flutter project generated by relax CLI.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  
  flutter_bloc: ^9.1.1
  bloc: ^9.2.0
  equatable: ^2.0.7
  get_it: ^8.0.3
  slang: ^4.14.0
  slang_flutter: ^4.14.0
  relax_orm: ^0.1.3
  relax_storage: ^1.0.1
  env:
    path: packages/env

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.0
  relax_orm_generator: ^0.1.5

flutter:
  uses-material-design: true
